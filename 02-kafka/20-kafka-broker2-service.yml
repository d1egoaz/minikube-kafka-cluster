---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka2-service
  name: kafka2-service
  namespace: kafka-ca1
spec:
  type: NodePort
  ports:
  - name: kafka-port
    port: 9092
    # nodePort: 30092
    targetPort: 9092
  selector:
    app: kafka
    id: "2"
