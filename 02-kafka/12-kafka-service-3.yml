---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka-service
  name: kafka-service3
  namespace: kafka
spec:
  type: NodePort
  ports:
  - name: kafka-port
    port: 9092
    nodePort: 30093
    targetPort: 9092
  selector:
    app: kafka
    id: "3"

